# 决定簿（overnight build）

> 规矩：不问用户，拿不准自己定，一行一条：**决定 · 原因 · 可逆吗**。
> 「可逆」= 改掉它要动多少东西：**易**（改一个常量/参数）、**中**（改一个模块 + 测试）、**难**（动契约或数据格式）。

| # | 里程碑 | 决定 | 原因 | 可逆 |
|---|---|---|---|---|
| D1 | M0 | 开发用 `C:\code\pmukit\.venv`（Python 3.11.7 + numpy 2.4.6 / scipy 1.17.1 / pytest 9.1.1），不复用老仓 venv | 老仓 venv 里有 PyQt5/skillbridge 等本仓禁止的依赖，共用会让"只许 stdlib+numpy+scipy"这条约束失效 | 易 |
| D2 | M0 | 提交闸匹配规则 = 大小写不敏感 + 非 `[A-Za-z0-9_]` 边界；token < 3 字符丢弃 | 要能在 `xi.VDD0P8_A` 里认出网名，又不能让 `ACMECHIP7X` 这种更长的词误报；短 token 噪声太大 | 易 |
| D3 | M0 | 提交闸命中后**改文件不改闸**：`design/*` 的真模拟电源网名→`VDDA_1V0`，`docs/CONTRACTS.md` 的三条真轨引脚名→`VDD0P8_A/B/C`，`tools/make_denylist.py` 例子里的真项目码→占位符 | 开工单硬约束第 1 条；这些名字来自真芯片，仓库是公开的 | 难（名字已进设计稿/文档，改回等于把客户名放回公开仓） |
| D4 | M0 | git hook 走 `core.hooksPath .githooks`（进仓、可版本化），不用 `.git/hooks` | `.git/hooks` 不进仓，换机器就失效；盒子和桌面要同一套闸 | 易 |
| D5 | M0 | 依赖用 `pyproject.toml`（PEP 621）+ `requirements.txt` 两份 | `pyproject` 给 `pip install -e .` 和入口点，`requirements.txt` 给 M12 的离线轮子流水线（`pip download` 读它） | 易 |
| D6 | M1 | 共享的四段式错误 `PmuError(what/why/do/where)` + `jsonio` 规范 JSON/哈希，先于五份契约写好，所有模块共用 | 错误格式是 UX_RULES 的硬规矩，哈希要在 Windows 和盒子上一致；不共用就会各写一套 | 中 |
| D7 | M1 | `run_id` 只哈希「网表 sha + 角 + 分析 + 激励 + 温度 + vset + load_key」，**不含** `reads`/`recipe`/`engine`/状态/耗时 | 这就是 resume：同一次仿真被重新计划（哪怕这次多读几个输出）必须撞上已完成的那一行 | 难（改了等于缓存全失效） |
| D8 | M1 | 重新计划时，已 `done`/`failed`/`imported` 的行保留状态、时间、psf 路径、engine、job_id，但刷新 `reads`/`recipe`/`netlist_path` | 计划变了不能抹掉已经花掉的机时；但「这次读什么」是计划说了算 | 中 |
| D9 | M1 | `consumes` 不加 SQL 外键（契约 §3 没有），孤儿三元组在 Python 层用四段式错误挡住 | 「契约文本优先」；同时仍开 `PRAGMA foreign_keys=ON` | 易 |
| D10 | M1 | `Block.observables` 由 `params` 推导而非手写 | 手写会和参数表漂移；推导后两者不可能不一致 | 易 |
| D11 | M1 | `Requirement.axes` 是**它自己的消费者**的并集，不是全局并集 | 否则只有偏置的项目会凭空继承轨的 load 轴 | 易 |
| D12 | M1 | 给轨 `dc` 块补一个 `vout_tc` 参数消费 `dc_temp` | 契约把 `dc_temp` 列在轨 `dc` 行，但原参数表没有任何参数读它 → 计划器永远不会安排扫温 | 易 |
| D13 | M1 | `SPEC_SHA` 把说明文字也算进去（note/why/tier 含义） | sha 的用途是钉住「这份交付件是哪一版规格产出的」，措辞变了也算变 | 易 |
| D14 | M1 | 数据集 `index.json` 每次改动就地原子重写（tmp → fsync → `os.replace`），`.npy` 先写 | 任意时刻被杀掉，目录都是可读的数据集；最坏丢最后一格，降级成 `never_run`——正是拟合器认得的状态 | 中 |
| D15 | M1 | 数据集只收浮点/复数 dtype | 整数没有 NaN，「没跑」的格子会被读成一个真实测量值 | 易 |
| D16 | M1 | `put` 成功会清掉该格子的 `mark_missing` 登记 | Run 屏的「重试」是一等流程；留着旧登记会让 `has()` 说谎 | 易 |
| D17 | M1 | 契约 0a 是**封闭 schema**：多一个顶层键就报错，不静默忽略 | 打错一个键会静默关掉一整条轴 | 易 |
| D18 | M1 | `DerivedConfig.sha()` **排除** `site` | engine/队列/CPU 数是装机属性；算进去会让同一次表征在桌面和红区哈希不同 | 易 |
| D19 | M1 | `tstop = max(8·t_settle, 50·edge)` | 默认值下 50×edge 这一项不起作用；但它保证再离谱的 `edge_s` 也不会产生比边沿还短的窗口 | 易 |
| D20 | M1 | 负载网格超过限流是**夹到限流**而不是丢点 | 夹住还剩四个不同电流，丢点就少一个负载条件 | 易 |
| D21 | M2 | 引脚的「地」用**子电路器件图的多源 BFS**从连线读出来：地引脚 = 台子里接到 `0` 的那些 PMU 引脚，每条轨/偏置归给最先到达它的那个地 | 契约要求「每个引脚接的地网直接从连线读」；分地要落到 `.va` 的 per-port 返回脚上。子电路体不在网表里时**明说**，不猜 | 中 |
| D22 | M2 | 单物理行的语句在**原始行**上改写（保留缩进和注释前的空格），续行语句才塌成一行 | 用户要能在配方里认出自己的网表；续行没有「原始单行」可留，塌成一行也顺手消灭了悬空反斜杠 | 易 |
| D23 | M2 | 角色前缀对不上 master（如 `IL_` 挂在 vsource 上）是**硬错误**，不是警告 | 读数学依赖 master：轨在电流注入下读电压，偏置在电压驱动下读探针电流。接反了数据是错的而不是缺的 | 易 |
| D24 | M4 | 计划的合并单位是**族**：analysis + 同一个激励源 = 同一次仿真；一个族跑它所有成员轴的**并集** | 这就是 AC 叠加的字面实现（轨 PSRR 和偏置 PSRR 是同一次电源注入）。并集不是高估：一份网表只有一个负载态/一个 VSET/一个温度，所以只要族里有人要扫负载，其余成员白捡这些点 | 中 |
| D25 | M4 | 负载轴是**共享的负载态** `L0..Ln`（第 k 个态让每条轨都取自己网格的第 k 点，网格短的保持末点），并带人类标签 `a=500u b=2m` | 一次仿真只能给每条轨一个电流，「每轨四个负载」不是自由轴。老仓的 L0-L5 不可读，这里标签随态走 | 中 |
| D26 | M4 | 扫温那条 run 的 `temp_c` 记 **NaN**（= 「这条不在单一温度上」），SQLite 存 NULL、读回来仍是 NaN | 连续扫温是一条 run 扫完整段，没有单一温度可填；`cell_text()` 显示 "T swept" | 易 |
| D27 | M4 | 成本模型可插拔，且第一轮真跑之后自动改用**本项目台账里的实测 CPU 秒中位数** | 估算值一开始必然是猜的；让它随真数据收敛，比调一张常数表诚实 | 易 |
| D28 | M1 | `.scs` section 库用 `//` 注释，不用 `*` | 库文件是 spectre 原生语法（`library`/`section`/`ahdl_include`），`*` 只在 SPICE 模式里是注释 | 易 |
| D29 | M1 | 摘要预算用「按优先级贪心、放不下就跳过继续试下一块」，最后按真实 trailer 大小再校正一轮；**D0 永不丢弃** | 和已定稿的 `design/Digest.dc.html` 行为一致；没有溯源的摘要没有意义 | 易 |
| D30 | M1 | D3（绿黄红）不在契约的优先级表里 —— 保持 `PRIORITY` 逐字不动，另给 D3 排在 D2 之后 | 它几百字节，却是用户唯一真正在问的那句"能不能信" | 易 |
| D31 | M1 | D2 每行是 `2 空格缩进 + JSON 片段 + '|'` | relay 粘贴会吃掉行尾空白；结尾的 `|` 让这一块免疫，且不靠 `#` 过滤（JSON 片段可能就以 `#` 开头） | 易 |
| D32 | M1 | 摘要正文非 ASCII 字符转写（`·`→`.`、`µ`→`u`、`→`→`->`），其余变 `?` | 摘要要穿过 relay 和 tcsh 终端；一个 UTF-8 字节就可能让 `sha256sum -c` 对不上 | 易 |
| D33 | M12 | 安装前缀 `$PMUKIT_PREFIX`（默认 `$HOME/pmukit`）、数据 `$PMUKIT_DATA`、解释器 `$PMUKIT_PYTHON`，**不抄老仓那条含真项目号的路径** | 公开仓不能出现客户路径；且 `/opt` 在共享盒子上不可写 | 易 |
| D34 | M12 | `apply` 额外支持 **git-clone 模式**：认出 `pyproject.toml + pmukit/` 就跳过完整性校验（本来就没打包），改走联网 pip | 明早的首跑清单第一条就是 `git clone` → `bash apply`，而 clone 出来没有 `wheels/` 也没有 `MANIFEST.json` | 易 |
| D35 | M12 | pmukit 本身**不**装进 venv：venv 只放 numpy/scipy，源码放 `$PREFIX/app`，启动器设 `PYTHONPATH` | 离线装包不必再带 setuptools/wheel；`update.sh` 换掉 `app/` 就完事，不用重装 | 中 |
| D36 | M12 | 打包用「文件系统遍历 + `git check-ignore`」而不是 `git ls-files` | 部署流水线必须在自己被提交之前就能打包自己 | 易 |
| D37 | M12 | 新增 `audit_wheels.py --deep`：扫轮子里 `.so` 的 `GLIBC_x.y` 字符串 | 文件名声称 2.17、里面的 `.so` 却要 2.28 的轮子，光看 tag 抓不到；这是没有 docker 时 auditwheel 的替身 | 易 |
| D38 | M12 | `tests/` 永不进包 | 开工单的拷贝清单里没有它，而且 fixtures 将来可能含真测量 | 易 |
| D39 | M11 | 帮助文本放 `pmukit/helptext.py`，CLI 和 web 都 import 它，不各写一份 | 验收条件就是"`pmukit help <screen>` = 帮助面板同一段文字"；两份副本迟早会漂移 | 易 |
| D40 | M11 | CLI 捕获 `PmuError` → 四段式打到 stderr + 退出码 2，**永不抛栈** | 用户看到的应该是"怎么办"，不是 traceback；退出码 2 让脚本能区分"用法/数据问题"和崩溃 | 易 |
| D41 | M11 | 解析前把 `--temps -40,25` 这类负号开头的值重拼成 `--temps=-40,25` | argparse 会把 `-40,25` 当未知选项；温度天然带负号，不能要求用户记得写 `=` | 易 |
| D42 | 0a | 补 `pmukit check`：开项目前的预检，只读不写 | 首跑清单最容易失败的一步是搭台子；`pmukit new` 会创建项目，用户需要一个「先看看对不对」的动作 | 易 |
| D43 | 0a | SKILL 生成脚本**明确标注未在桌面执行过**，并写明「判据是校验器不是脚本」 | 桌面没有 Virtuoso；把一个没跑过的脚本说成能用，会浪费用户一个早上 | 易 |
| D44 | M2 | 黑盒 include 时引脚名回退成网名，**按位置去重**（`0`、`0#8`、`0#9`） | 三个地都接 `0`，原来会在字典里互相覆盖成一个引脚 —— 分地信息静默丢失 | 易 |
| D45 | M3 | PDK 的 BSIM3 卡片不能放在 `toplevel.scs` 的文件作用域，必须每个 section 里 `include "models_bsim3.scs"` | **实测**：用 `section=<x>` 包含一个文件时，写在任何 section 之外的语句会被静默丢弃 | 中 |
| D46 | M3 | fixture 的地脚在 PMU 实例行上直接接 `0`（而不是用 0 V 源接到命名网上） | `netlist.py` 和模板都按 `net in ("0","gnd","gnd!")` 认地；分地在**子电路内部**仍然是真的，那才是 BFS 读的东西 | 易 |
| D47 | M3 | 把 ngspice 源文件 vendored 进 `tests/fixtures/ldo_gt/ngspice/` | 没有私有老仓也能复现转换（盒子上也能跑 `--check`）；有测试断言已提交的 `.scs` 与重新转换逐字节相同 | 易 |
| D48 | M3 | 模型卡片留在 `simulator lang=spice` 块里，subckt 体翻成 Spectre 语言并把**每个**量纲后缀展开成显式指数 | `meg`/`M`/`m` 是两个读者之间唯一会静默算错的陷阱 | 中 |
| D49 | M3 | 开工单说 14 个合成 LDO 但列了 15 个名字 → **15 个全转** | 少转一个等于少一个回归目标；多转的成本是零 | 易 |
| D50 | M9 | 摘要里的 cell 用 `dataset.cell_key()` 那个字符串（`tt/25C/vset3/5.0e-04A`），`parse_cell_key` 反解；也接受 dict | 已有的往返函数，不用再发明一套 cell 记法；两边同一份代码 | 易 |
| D51 | M9 | 重建数据集时**只放 ground truth**，模型曲线跟着 `params` 走 | 数据集是测量；模型是拟合结果。混进去就没法回答"模型和真件差多少" | 易 |
| D52 | M9 | 整块被丢弃时另写 `dropped.json`（因为没有变量可挂 `missing` 行） | 否则"盒子丢了一整块"这件事在桌面是**隐形**的 —— 正是契约禁止的静默截断 | 易 |
| D53 | M9 | 桌面没装拟合器时 `reproduce` 不报错，如实说明并交出盒子的参数 | 桌面可能是个只装了标准库的机器；凭 D2 就能重新发射 `.va`，不该因为缺拟合器就什么都拿不到 | 易 |
| D54 | M5 | `fake` 后端跑出来的 run 记 `done`（engine=`fake` 标明来路），**不记 `imported`** | 契约里 `imported` 专指"用户自己已有的外部结果 + `source_path`"；混用会污染 Plan 屏的"已覆盖"计数和成本账 | 易 |
| D55 | M5 | `dry_run` 让台账停在 `planned` 而不是 `skipped_cached` | `skipped_cached` 属于"有结果"，会让这条 run 从 `not_run()` 里消失 —— 干跑没有结果 | 易 |
| D56 | M5 | 传输用 tar-over-ssh（stdlib `tarfile`），不用 rsync/scp | Git Bash 没有 rsync；scp 会在冒号处切开 `C:/…`。下行 base64 编码，免得远端登录横幅污染数据 | 中 |
| D57 | M5 | 噪声单位**从 PSF 的 TRACE 类型读**，不假设 | `V/sqrt(Hz)` 和 `V^2/Hz` 差 1e12；文件没说清就硬报错 | 易 |
| D58 | M5 | 瞬态存在固定均匀栅格（2001 点）上，极值保持误差超 0.1 % 就报告 | 契约 2 给一个变量一个坐标，而求解器每个角选的点不一样 | 中 |
| D59 | M5 | Donau 的站点事实从环境变量读（`PMUKIT_ALPS_ROOT` 等），仓里不写任何真路径/账号 | 公开仓 | 易 |
| D60 | M6 | 连续温度律的锚点统一取 `T_REF_C = 25 °C` | 参数表要能脱离上下文求值；老仓用的 55 °C 是那颗芯片的表征温度，不是通用锚点 | 中 |
| D61 | M6 | 上下文（`zout`、`vc`、`g0`、`final`）走 kwargs，**不复制进 `params`** | `params` 的键必须逐字等于 spec 的参数名（有测试对每个块断言）；复制一份就会出现两个真相 | 易 |
| D62 | M6 | `dropout`/`ilimit` 在扫描没离开调整区时返回 `None` + 原因 | 外推一个限流值比承认没测到危险得多 | 易 |
| D63 | M6 | 识别性除了搬来的列范数 `unidentifiable`，新增 `poorly_determined`，且 `sigma` 用**截断**伪逆 | 不截断的话，一个死旋钮会把整块都报成不可辨识 | 易 |
| D64 | M2 | 反接的约定源照样归类，但记 `src_reversed` 并在 notes 说明极性反转；一条网两个约定源则报错 | 人手画很容易把 `(0 pin)` 画成 `(pin 0)` 的反面；认不出来比认出来再说明极性更糟 | 易 |
| D65 | M4 | 简单角写法只改**真有这个 section** 的 include；读不到的照契约改但标注"未验证"，并报告每个角落到哪几个文件 | 字面执行契约会把 RC skew 文件也改成 `tt`，Spectre 直接报 `No section found with name 'tt'` —— 这是 M5 真跑时撞出来的 | 中（偏离了契约字面，但契约本意是"设工艺角"） |
| D66 | M10 | `GET …/plan` 是**同步**的（带 config/derived/netlist sha 的缓存），不走后台 job | 编译计划是 0.5 秒的纯算术；一个时而返回 job 时而返回数据的 GET 会让契约含糊 | 易 |
| D67 | M10 | "跳过"= 台账 `planned` + 理由写进 `error`；"杀掉" = `failed` + "killed from the Run screen" | 契约把状态集合钉死了，不能为了界面加新状态；`not_run()` 本来就会把它报出来 | 中 |
| D68 | M10 | 拟合了但还没被 `verify` 判过的块，给一个**单独的**等级 `fitted`（中性蓝），不给 `green` | "有数字"不等于"能信"；默认成绿色正是这个工具要消灭的那种乐观 | 易 |
| D69 | M10 | 种子配置把电源脚和 EN 脚设成 `model` 而不是 `ignore` | `derive()` 会丢掉 `ignore` 的脚，于是整组 PSRR 注入和 EN 斜坡**静默消失** | 易 |
| D70 | M10 | Windows 上关掉 `allow_reuse_address` | Windows 的 SO_REUSEADDR 意思是"抢占一个正在监听的端口"；第二个 `pmukit ui` 会劫持而不是自动换端口 | 易 |
| D71 | M10 | 绑 loopback 时拒绝 Host 不是 loopback 的请求（403） | DNS rebinding 防护；`--host` 显式放开 | 易 |
| D72 | 全局 | `.tmpdata/`、`pmukit_data/`、`*_data/` 按**名字**加进 `.gitignore` | 这些目录一旦指向真台子就装着真测量；公开仓不能靠"希望没人建" | 难（数据规矩） |
| D73 | 全局 | `pyproject.toml` 补 `[tool.setuptools.package-data] "pmukit.web" = ["index.html"]` | 不补的话装出来的 wheel 没有页面，`pmukit ui` 在盒子上是空白的 | 易 |
| D74 | M4/M11 | 网表里**相对** include 的顶层目录随每个 run 目录一起送到远端 | 台子相对 include PDK，run 目录在别处 → `SFE-868 Can not open input file`。绝对路径不动（拷整个 PDK 更糟） | 易 |
| D75 | M4 | 只声明一个温度时**不生成**扫温 run，并明说"读它的参数会被报成 NOT RUN" | Spectre 拒绝 start==stop 的扫描；而编一段用户没要求的温度范围是更坏的答案 | 易 |
| D76 | M1/M4 | 没声明 `--load` 的轨，负载扫描量程取 **0 .. 2× 台子自己的典型负载** | 负载调整率/dropout/限流仍然要测；这个量程来自网表，不是猜用户的模块 | 易 |
| D77 | M5 | `fake` 后端的 DUT 改成**物理自洽**：Zout 在 DC 有限（R_dc+jωL ∥ esr+1/jωC）、PSRR = i_c·Zout、轨噪声 = Zout 整形的 Norton 电流 | 原来 Zout 在 DC → 0 而 PSRR 平坦，等于要求 i_c 有个 DC 极点，任何有理式都拟不出来 —— 每次拟合都得 ~20 dB，看着像拟合器的 bug，其实是**假件不物理**。改完 PSRR 30 dB→0.024 dB、噪声 5.5 dB→0.0069 dB | 中 |
| D78 | M1/M6 | 给偏置 `psrr` 块加一个 `c_ft`（电源→输出的**馈通电容**），作为 keep-best 的可选自由度 | 真 Spectre 实测：合成 PMU 的 PTAT 参考，电源→电流传递在 10 kHz 以下平坦在 357 nS，到 1 GHz **升了 500 倍**到 173 µS（= 28 fF）。原来的块只有"单极点下降"形，拿下降形去拟合一条上升曲线，代价 ~22 dB —— 而这正好是产生 VCO 杂散的那个频段。加上之后 21.8 → 9.49 dB。keep-best 保证平坦的传递买不到这个旋钮 | 中（动了契约 1 的参数表 → `SPEC_SHA` 变了） |
| D79 | M7 | `G0` 限带的余量取 **200 × `care_up_to_hz`**（实测选的，不是拍的） | 余量 50 时 C_ft 轨代价 **0.021 dB**（平坦项的相位旋转和 s·C_ft 的正交分量拍频），余量 200 时 **0.0053 dB**。10 MHz 的 PSRR 带正好落在 2 GHz —— 就是真振荡器 HB 上手工验证过的那个值 | 易 |
| D80 | M7 | lint 的度量是**每个节点的总导纳动态范围**，但按元件点名 | 单个元件量级极端不是缺陷（一个拟合出来的 20 µH 支路在 20 GHz 真的就是 2 MΩ）；出事的是**整行都下溢**的节点。阈值 1e6 是合法物理元件够得着的，所以它**报告并点名**，不拒绝发射 | 易 |
| D81 | M7 | `ls` 档默认 **关**：`load_en_<rail>` 默认 0，直到 `deliver(ls_default_on=[...])` 说 HB 首步残差体检过了 | 开工单的硬约束：大信号项必须过体检才允许默认开。体检是 M8 | 易 |
| D82 | M7 | `vset` 选择器只作用在轨的 DC 上；其余块烘在默认档，`.va` 头写明 | VSET 就是设输出电压的；给每个档复制一整套 Zout/PSRR/噪声网络会让节点数翻倍，去换一个没人单独测过的敏感度 | 中 |
| D83 | M7 | `hb_robust=False` 现在会**真的发射被否决的 R-L-C**，作为有记录的诊断用法 | 这是在一个真发射出来的模型上演示 lint 的唯一办法；`deliver()` 会拒绝把它标成 HB-ready | 易 |
| D84 | M7 | 没有实测直流值的 stub 引脚是**弱连**，不是驱动，并进"未跑"清单 | 契约说 stub 发成"该脚直流值的理想源"，但约定源给的是**对偶量**（轨脚上的 `IL_` 是电流，需要的是电压；偏置脚上的 `VB_` 是电压，需要的是电流）。编一个值比弱连危险 —— 见下面的未决项 | 中 |
| D85 | 0a | 契约 0a 增加可选键 `stub_dc`（轨填伏特、偏置填安培），CLI 是 `--stub PIN=值` | 契约要求 stub 发成"该脚直流值的理想源"，但约定源给的是**对偶量**，而 stub 按定义不跑仿真 —— 这是网表唯一给不出的数。缺了就弱连 + 报告写明，绝不编。**注意顺序反了**：这次是代码先改、契约文档后补（真跑交付时撞出来的），规矩是先改文档 | 中（动了契约 0a） |
| D86 | M11 | `emit.deliver(root=...)` 收的是 **`$PMUKIT_DATA` 根**（它自己接 `<project>/deliver/<stamp>`），CLI 原来传的是项目目录 → 路径里项目名重复 | 真跑 `pmukit deliver` 时撞出来的 | 易 |
| D87 | M11 | 加 `pmukit site`（看/改 engine、queue、cpus、账号），并在首跑清单第 0 步显式要求设它 | 站点配置原来只能靠改 `site.json` 或环境变量；而默认值 `spectre_ssh`+`ewave-vm` 是**桌面的**默认值，在盒子上是错的 —— 这是明早最容易踩的一脚 | 易 |
| D88 | M8 | HB 体检的判据是**牛顿第一步之后**的残差，不是「初始残差」 | 实测：`ls` 项开与不开，初始残差逐位相同 —— 因为该项在工作点上值和斜率都为零，而 HB 初值就是工作点。两个数都报：动了**初始**残差的项是更糟的缺陷 | 中 |
| D89 | M8 | 体检门天然严格，且在输出里就这么说：FAIL 的含义是「这一项让消费者多花牛顿步」，不是「它把 HB 弄炸了」 | 全关的基线是纯线性模型，一步收敛到数值噪声，任何被激活的非线性项都过不了 10× —— 把这条当成「坏」会误导；**这正是 `ls` 档的定义** | 中 |
| D90 | M8 | `ls_default_on` 要过**两道**门：HB 体检 **且** 该块没被判 red | 过了条件门只说明这一项不会把求解器弄坏，没说明它是**对的** | 易 |
| D91 | M8 | 识别性封顶到 yellow 有一个例外 `_inert()`：被关掉的参数（值为 0、支路电阻停在 1e8 哨兵、比最响段低 60 dB 的 bank 段、零增益的伴随参数）不算 | 不加这条，工具里**每一个**块都是 yellow（demo PMU 上原始门在 100 % 的 psrr/noise/zout 格子上触发，而绝大多数是 `Rb=1e9`、`pc_gain=0` 这种关掉的旋钮） | 中 |
| D92 | M8 | 噪声门限（绿 2 dB / 黄 6 dB）**故意比小信号松** | `spec.py` 里记着一条挂起的 ~3 dB 噪声相关性误差，没人测过。**比一个已知的未测系统误差还紧的门是做样子** | 易 |
| D93 | M8 | 回归基线里记 engine、台子条件，并给 15 个里的 14 个写上 `known_hard` | 一个不能表达「这个本来就难」的回归套件会教出错误的结论；跨 engine 的比较**直接拒绝**，不糊弄 | 易 |
| D94 | M8 | `fake`/`dry_run` 被**拒绝**用于 HB 和振荡器工作（四段式理由），而不是让它们编残差 | 一个编出来的收敛判据比没有判据更危险 | 易 |
| D95 | M8 | `en/ramp` 是挂在轨/偏置上的**额外一个块**，不是一种端口类型 | `tran_en.<rail>` 是**那条轨**上电的曲线；EN 脚自己没有曲线。按「加一个 en 端口」修会把 stub 端口也拖进来拟合 | 中 |
| D96 | M11 | `pmukit deliver` 自动读旁边的 `verify.json`（等级 + `ls_default_on` + `not_run`） | 否则报告写「nothing was graded」，而等级就躺在同一个目录里 | 易 |
| D97 | M11 | 识别性封顶改成「**没钉住 且 有影响**」：拟合器对每个被标记的参数量 `influence_db`（数据容许的所有移动——单独走 + 松参数沿 (JᵀJ)⁺ 联合方向走，任何数据点变化不超过 0.5 dB——在包络频带内对预测幅度的最大改变），超过该度量的绿限才 hold；没量过的旗仍然 hold；包络超出数据时**所有**参数都量（没被标记但决定包络外答案的参数也 hold）。另：`knee_side="none"` 时不再对 vknee/knee_p/vhi 做门 | fake demo 上每个 zout/psrr/noise/idc 格子都被 hold（Rpl、G0、amp_i、不存在的膝），分数 0.04 dB 对 1 dB 限，永远到不了 OK。小信号块只烤在一个格子上、不跨格插值，所以一个自由的数只能通过**使用频带**误导消费者；数据覆盖该频带时，「自由」按定义就「改不了答案」 | 中 |
